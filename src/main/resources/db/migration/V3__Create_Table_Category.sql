-- Dumping structure for table salesapp.tb_category
CREATE TABLE IF NOT EXISTS `tb_category` (
                                             `id` bigint NOT NULL AUTO_INCREMENT,
                                             `name` varchar(255) DEFAULT NULL,
                                             PRIMARY KEY (`id`)
);